{% macro ajax(method, path, param) %}
    <script>
      $(document).ready(function() {
        $("#ajaxButton").click(function() {
          var parametre = "{{ param|escape('js') }}";
          if (validateParameter(parametre)) {
            $.ajax({
              url: "{{ __path(path)|escape('js') }}",
              type: method.toUpperCase(),
              data: {
                parametre1: parametre,
              },
              success: function(response) {
                $("#result").html(response);
              }
            });
          } else {
            console.error('Paramètre non valide');
          }
        });
      });
    
      function validateParameter(param) {
        return param.length > 0;
      }
    </script>
    {% endmacro %}
    